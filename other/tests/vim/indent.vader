Given nim:
  import
  sequtils,
  streams

  type Obj = object
  x, y: int

  proc nope =
  echo "foobar"
  nope()

Do:
  gg=G

Expect:
  import
      sequtils,
      streams

  type Obj = object
      x, y: int

  proc nope =
      echo "foobar"
  nope()
